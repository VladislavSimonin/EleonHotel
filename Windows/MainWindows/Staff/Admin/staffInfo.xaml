<Window x:Class="EleonHotel.Windows.MainWindows.Admin.staffInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EleonHotel.Windows.MainWindows.Admin"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Информация о сотрудниках" MinHeight="500" MinWidth="1200">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Background="#22220398" CanContentScroll="True" VerticalScrollBarVisibility="Auto">
            <DataGrid Name="StaffDataGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      GridLinesVisibility="Horizontal"
                      AlternatingRowBackground="#FFE0E0E0"
                      RowHeight="40"
                      HeadersVisibility="Column"
                      SelectionMode="Single"
                      SelectionChanged="StaffDataGrid_SelectionChanged"
                      d:ItemsSource="{Binding}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID сотрудника" Binding="{Binding employee_id}" Width="80"/>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding surname}" Width="120"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding name}" Width="120"/>
                    <DataGridTextColumn Header="Отчество" Binding="{Binding patronymic}" Width="120"/>
                    <DataGridTextColumn Header="Телефон" Binding="{Binding phone}" Width="120"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding email}" Width="200"/>
                    <DataGridTextColumn Header="Начало смены" Binding="{Binding shift_time_start, StringFormat={}{0:hh\\:mm}}" Width="100"/>
                    <DataGridTextColumn Header="Конец смены" Binding="{Binding shift_time_end, StringFormat={}{0:hh\\:mm}}" Width="100"/>
                    <DataGridTextColumn Header="Должность" Binding="{Binding position_name}" Width="150"/>
                    <DataGridTextColumn Header="Зарплата" Binding="{Binding salary, StringFormat={}{0:F2}}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Добавить" Width="120" Height="30" Margin="5" Click="AddButton_Click" Background="#FF3CDC5C"/>
            <Button Content="Редактировать" Width="120" Height="30" Margin="5" Click="EditButton_Click" Background="#FF3C9CDC"/>
            <Button Content="Удалить" Width="120" Height="30" Margin="5" Click="DeleteButton_Click" Background="#FFDC3C3C"/>
            <Button Content="Закрыть" Width="120" Height="30" Margin="5" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
