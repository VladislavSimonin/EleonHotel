<Window x:Class="EleonHotel.Windows.MainWindows.Admin.addEmployee"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EleonHotel.Windows.MainWindows.Admin"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="Добавление сотрудника" MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Выберите пользователя для добавления в качестве сотрудника:" 
                   FontSize="16" Margin="10" HorizontalAlignment="Center"/>

        <ScrollViewer Grid.Row="1" Background="#22220398" CanContentScroll="True" VerticalScrollBarVisibility="Auto">
            <DataGrid Name="UsersDataGrid" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      SelectionChanged="UsersDataGrid_SelectionChanged"
                      d:ItemsSource="{Binding}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Фамилия" Binding="{Binding surname}" Width="*"/>
                    <DataGridTextColumn Header="Имя" Binding="{Binding name}" Width="*"/>
                    <DataGridTextColumn Header="Отчество" Binding="{Binding patronymic}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="Добавить" Width="120" Height="30" Margin="5" Click="AddButton_Click" Background="#FF3CDC5C"/>
            <Button Content="Отмена" Width="120" Height="30" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
